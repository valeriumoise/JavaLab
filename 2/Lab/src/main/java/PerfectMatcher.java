public class PerfectMatcher extends Matcher {

    /**
     * Builds a new instance of the Perfect Matcher
     *
     * @param problem The problem for witch we want to apply the matching
     */
    public PerfectMatcher(Problem problem) {
        super(problem);
    }


}
